services:
  youtube-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      - PORT=3000
      - NODE_ENV=development
    ports:
      - "${PORT:-3000}:3000"
    restart: unless-stopped
